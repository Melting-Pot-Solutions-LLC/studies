// generating random numbers
getRandomInteger(min, max)
{
  console.log("getRandomInteger");
  return Math.floor(Math.random() * (max - min) + min);
}

//convert a number to a string
new_time = "click me";
this.new_time = ""+(this.getRandomInteger(1000, 4000));
